import { SyncAction } from '../utils/actionHelpers';
export interface IAction<T extends Redux.Action> {
    prototype: T;
}
export declare type Reducer<State, ActionType extends SyncAction> = (state: State, action: ActionType) => State;
export declare class ReducerBuilder<State> {
    private actions;
    private initState;
    init(state: State): this;
    handle<T extends SyncAction>(actionType: IAction<T>, actionBody: Reducer<State, T>): this;
    build(): (state: State, action: SyncAction) => State;
}
